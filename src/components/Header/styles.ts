import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#0D0D0D',
        alignItems: 'center',
    },

    logo:{
        marginTop: 64,
        marginBottom: 64
    }
})