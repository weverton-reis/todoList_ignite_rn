import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingVertical: 48,
        borderTopWidth: 1,
        borderTopColor: '#333333'
    },

    text:{
        fontSize: 14,
        color: '#808080'
    }
})